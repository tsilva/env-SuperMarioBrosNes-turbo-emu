## PROJECT PURPOSE

Provide reinforcement-learning researchers with a high-throughput, vectorized Gymnasium environment for the supported Super Mario Bros NES mapper 0/NROM workload while preserving deterministic lane-isolated execution, training and playback compatibility, and reproducible performance evaluation.

## PROJECT REQUIREMENTS

- Use `env-SuperMarioBrosNes-turbo-emu` as the project and GitHub repository name, `env-supermariobrosnes-turbo-emu` as the Python distribution name, and `env_supermariobrosnes_turbo_emu` as the public Python import package; current project-owned identities must not use any former project, distribution, import, or command identifier.
- Support only Super Mario Bros NES on mapper 0/NROM unless broader emulator scope is deliberately added and validated without regressing the specialized workload.
- Keep ROM content out of the repository and distributions, allow users to import it through module and installed command entry points and run without supplying its path through the Stable Retro-compatible `RETRO_DATA_PATH` layout, and require canonical validation and performance comparisons to use ROM SHA-256 `f61548fdf1670cffefcc4f0b7bdcdd9eaba0c226e3b74f8666071496988248de`.
- Keep all training implementations and recipes outside this repository, expose no package or checkout trainer entry points, and document version-pinned one-command published-GradLab training recipes that accept a local ROM path and show live progress.
- Preserve the public `env_supermariobrosnes_turbo_emu` package, `EnvSuperMarioBrosNesTurboEmuVecEnv`, `Actions`, metadata-derived action meanings and sets, state helpers, and manual and policy playback entry points.
- Conform to the Gymnasium `VectorEnv` reset and step return contracts, permanently report disabled autoreset, block further stepping of terminal lanes, and support selective reset through `options["reset_mask"]`.
- Keep every vector lane deterministic under seeding and independently emulated; resetting selected lanes must leave every unselected lane's emulator state, random stream, observation history, sticky action, and counters unchanged.
- Keep seeded random reset-NOOP advancement lane-local and opt-in, with zero as the disabled default.
- Allow live vector lanes to be captured and selected lanes to be restored from reusable same-instance snapshots without advancing emulation or changing unselected lanes.
- Preserve `Actions.ALL`, `Actions.FILTERED`, Stable Retro-compatible 36-way `Actions.DISCRETE`, `Actions.MULTI_DISCRETE`, and exact named or caller-supplied button-label action tables through `use_restricted_actions`, together with grayscale or RGB observations, frame skip, optional max-pooling, crop removal or masking, resize, frame stacking, and CHW or HWC layouts.
- Preserve constructor-time initialization from one packaged or named state, path, or byte payload; expose an immutable ordered catalog containing every saved state selectable by an environment instance; reset selected lanes to explicit catalog indices with index zero as the deterministic default; report active indices; and keep state sampling, reset timing, and lane routing outside the provider, including for packaged states from `Level1-1` through `Level1-4`.
- Preserve Stable Retro-compatible native game-over termination and expose raw lives, level, score, time, position, and scrolling signals so downstream systems can own life-loss, flag or level-completion events, additional termination, reward shaping, and outcomes; expose opt-in research signals only as documented semantic values and never return unrequested game-state keys.
- Keep `play.py` and `smb-turbo play` as exact discoverable-state-keyed action-run playback workflows; playback without a state must start from `Level1-1`, support an explicitly uncapped rate, automatically select matching canonical policies as levels change, preserve legacy Beam-over-JERK discovery, and not require a neural-network framework.
- Keep the public `step()` path throughput-oriented across batched emulation, native reward and termination, preprocessing, frame stacking, and Gymnasium infos; performance claims must evaluate that path with matched workloads and statistically validated, reproducible comparisons, and neutral feature changes must retain at least a `0.99` median candidate-to-baseline ratio with a bootstrap 95% lower bound of at least `0.98`.
- When explicitly asked to benchmark the latest release, use the newest stable PyPI version and publish its evidence to the project Hugging Face benchmark dataset only after the canonical TurboBench run is official and verified; link the immutable publication from the project benchmark documentation.
- Require every release’s exact final wheel for the canonical parity host to pass an immutable TurboBench parity profile against the pinned original Stable Retro release for the canonical ROM and public `Level1-1` through `Level1-4` state corpus, covering raw and processed observations, actions, rewards, episode boundaries, canonical CPU RAM, shared semantic signals, vector-lane behavior, and snapshot-restored continuation.
- Provide a thin TurboBench command for isolated quick parity of current repository work; checkout results must remain diagnostic, and this repository must not implement cross-provider comparison logic.
- Use provider-owned internal checks to prove that canonical behavior remains identical across supported binary platforms.
- Preserve the `cp39-abi3` extension contract and validate releases on CPython 3.14; publish binary distributions only for Apple-silicon macOS and x86-64 Linux.
